import Navbar from "@/components/Navbar";
import Sidebar from "@/components/Sidebar";

const HomePage = () => {
  return (
    <section>
      <Navbar />
      <Sidebar />
    </section>
  );
};

export default HomePage;
